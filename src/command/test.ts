import * as h from "./commandHelper";
const test = {
  run: async () => {
  // let styles = {};
  // figma.getLocalPaintStyles().forEach(style => {
  //   styles[style.name] = style.key;
  // });
  // console.log(JSON.stringify(styles));
    console.log(h.selection(0));
  }
}
export default test;